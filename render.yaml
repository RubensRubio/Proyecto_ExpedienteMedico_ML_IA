services:
  - type: web
    name: asistente-clinico
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.api:app --host 0.0.0.0 --port 8080
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.0
